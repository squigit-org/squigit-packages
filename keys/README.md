# Signing Keys

Release CI publishes:

- `squigit-packages.asc` (ASCII armored public key)
- `squigit-packages.gpg` (binary public key)

The private key is never committed here; it is loaded from CI secrets.
