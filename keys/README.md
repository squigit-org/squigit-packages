# Signing Keys

Release CI publishes:

- `squigit-distribution.asc` (ASCII armored public key)
- `squigit-distribution.gpg` (binary public key)

The private key is never committed here; it is loaded from CI secrets.
