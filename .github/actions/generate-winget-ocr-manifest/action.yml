name: "Generate Winget OCR Manifest"
description: "Generates Winget manifest files for the Squigit OCR archive"
inputs:
  ocr-identifier:
    description: "Winget OCR identifier"
    required: true
  ocr-version:
    description: "OCR semantic version"
    required: true
  ocr-name:
    description: "OCR display name"
    required: true
  publisher:
    description: "Publisher string"
    required: true
  short-description:
    description: "Short OCR description"
    required: true
  installer-url:
    description: "Installer ZIP URL"
    required: true
  installer-sha256:
    description: "Installer ZIP SHA256"
    required: true
  command-alias:
    description: "Command alias exposed by winget"
    required: true
  nested-installer-path:
    description: "Relative path to executable inside ZIP"
    required: true
  architecture:
    description: "Winget architecture value"
    required: false
    default: "x64"
  ocr-locale:
    description: "Locale for defaultLocale manifest"
    required: false
    default: "en-US"
  license:
    description: "License string"
    required: false
    default: "Apache-2.0"
  manifest-version:
    description: "Winget manifest schema version"
    required: false
    default: "1.6.0"
  archive-binaries-depend-on-path:
    description: "Set true when archive portable binaries require install-dir PATH instead of link shims"
    required: false
    default: "false"
  output-dir:
    description: "Directory where manifest files will be written"
    required: false
    default: "target/winget-manifest"
outputs:
  manifest-dir:
    description: "Directory containing generated manifest files"
    value: ${{ steps.generate.outputs.manifest_dir }}
runs:
  using: "composite"
  steps:
    - name: Generate Manifest Files
      id: generate
      shell: bash
      env:
        OCR_IDENTIFIER: ${{ inputs.ocr-identifier }}
        OCR_VERSION: ${{ inputs.ocr-version }}
        OCR_NAME: ${{ inputs.ocr-name }}
        PUBLISHER: ${{ inputs.publisher }}
        SHORT_DESCRIPTION: ${{ inputs.short-description }}
        INSTALLER_URL: ${{ inputs.installer-url }}
        INSTALLER_SHA256: ${{ inputs.installer-sha256 }}
        COMMAND_ALIAS: ${{ inputs.command-alias }}
        NESTED_INSTALLER_PATH: ${{ inputs.nested-installer-path }}
        ARCHITECTURE: ${{ inputs.architecture }}
        OCR_LOCALE: ${{ inputs.ocr-locale }}
        LICENSE: ${{ inputs.license }}
        MANIFEST_VERSION: ${{ inputs.manifest-version }}
        ARCHIVE_BINARIES_DEPEND_ON_PATH: ${{ inputs.archive-binaries-depend-on-path }}
        OUTPUT_DIR: ${{ inputs.output-dir }}
      run: |
        set -euo pipefail

        mkdir -p "${OUTPUT_DIR}"

        VERSION_FILE="${OUTPUT_DIR}/${OCR_IDENTIFIER}.yaml"
        LOCALE_FILE="${OUTPUT_DIR}/${OCR_IDENTIFIER}.locale.${OCR_LOCALE}.yaml"
        INSTALLER_FILE="${OUTPUT_DIR}/${OCR_IDENTIFIER}.installer.yaml"
        VERSION_SCHEMA_URL="https://aka.ms/winget-manifest.version.${MANIFEST_VERSION}.schema.json"
        LOCALE_SCHEMA_URL="https://aka.ms/winget-manifest.defaultLocale.${MANIFEST_VERSION}.schema.json"
        INSTALLER_SCHEMA_URL="https://aka.ms/winget-manifest.installer.${MANIFEST_VERSION}.schema.json"
        ARCHIVE_FLAG="$(printf '%s' "${ARCHIVE_BINARIES_DEPEND_ON_PATH}" | tr '[:upper:]' '[:lower:]' | xargs)"

        if [ "${ARCHIVE_FLAG}" != "true" ] && [ "${ARCHIVE_FLAG}" != "false" ] && [ -n "${ARCHIVE_FLAG}" ]; then
          echo "archive-binaries-depend-on-path must be true or false, got: ${ARCHIVE_BINARIES_DEPEND_ON_PATH}" >&2
          exit 1
        fi

        ARCHIVE_LINE=""
        if [ "${ARCHIVE_FLAG}" = "true" ]; then
          if ! [[ "${MANIFEST_VERSION}" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "manifest-version must be semver when archive-binaries-depend-on-path=true (got ${MANIFEST_VERSION})" >&2
            exit 1
          fi
          MANIFEST_MINOR="$(printf '%s' "${MANIFEST_VERSION}" | awk -F. '{print $2}')"
          if [ -z "${MANIFEST_MINOR}" ] || [ "${MANIFEST_MINOR}" -lt 9 ]; then
            echo "archive-binaries-depend-on-path=true requires manifest-version >= 1.9.0 (got ${MANIFEST_VERSION})" >&2
            exit 1
          fi
          ARCHIVE_LINE="ArchiveBinariesDependOnPath: true"
        fi

        cat > "${VERSION_FILE}" <<EOF
        # yaml-language-server: \$schema=${VERSION_SCHEMA_URL}
        PackageIdentifier: ${OCR_IDENTIFIER}
        PackageVersion: ${OCR_VERSION}
        DefaultLocale: ${OCR_LOCALE}
        ManifestType: version
        ManifestVersion: ${MANIFEST_VERSION}
        EOF

        cat > "${LOCALE_FILE}" <<EOF
        # yaml-language-server: \$schema=${LOCALE_SCHEMA_URL}
        PackageIdentifier: ${OCR_IDENTIFIER}
        PackageVersion: ${OCR_VERSION}
        PackageLocale: ${OCR_LOCALE}
        Publisher: ${PUBLISHER}
        PackageName: ${OCR_NAME}
        License: ${LICENSE}
        ShortDescription: ${SHORT_DESCRIPTION}
        ManifestType: defaultLocale
        ManifestVersion: ${MANIFEST_VERSION}
        EOF

        cat > "${INSTALLER_FILE}" <<EOF
        # yaml-language-server: \$schema=${INSTALLER_SCHEMA_URL}
        PackageIdentifier: ${OCR_IDENTIFIER}
        PackageVersion: ${OCR_VERSION}
        InstallerType: zip
        NestedInstallerType: portable
        ${ARCHIVE_LINE}
        Installers:
          - Architecture: ${ARCHITECTURE}
            InstallerUrl: ${INSTALLER_URL}
            InstallerSha256: ${INSTALLER_SHA256}
            NestedInstallerFiles:
              - RelativeFilePath: ${NESTED_INSTALLER_PATH}
                PortableCommandAlias: ${COMMAND_ALIAS}
        ManifestType: installer
        ManifestVersion: ${MANIFEST_VERSION}
        EOF

        echo "manifest_dir=${OUTPUT_DIR}" >> "${GITHUB_OUTPUT}"
